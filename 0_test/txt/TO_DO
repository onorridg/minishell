echo '"'"''$PATH''"'"': 			https://stackoverflow.com/c/42network/questions/1983
echo echo '""''$PATH''"'"''"
echo echo "$USER|'"'$USER'|echo "|" 
echo "$USER|'"'$USER'|echo "|test" | 
ls -al | grep ^d | grep 13 | grep 14:21 | grep 136 | grep exit
exit command dose not work

"➜ root@mac:# clear
TERM environment variable not set."

ls -al | grep "$word "

"
➜ root@mac:# kek=test
➜ root@mac:# test=42
➜ root@mac:# export $kek
"

cd $HOME

"export $kek
bash: export: `42': not a valid identifier"

< test < test grep " " < kek.txt |

<< EOF grep "key" < kek.txt

< result.txt grep ^d | grep 12 | wc -l > output

< result.txt  grep 12  | grep 45 | wc -l

< result.txt  grep 12  | wc -l

"
root@mac:# chmod 000 output
pipes: 1
root@mac:# ls > output
pipes: 1
minishell: output: Permission denied
root@mac:# chmod 000 output
pipes: 1
[1]    12759 segmentation fault (core dumped)  ./minishell
"

"
root@mac:# ls > output
pipes: 1
minishell: output: Permission denied
root@mac:# chmod 777 output
pipes: 1
[1]    13500 segmentation fault (core dumped)  ./minishell
"

press ctrl+d when open pipe                 --> segfault

"echo -n"                                   --> segfault

< tets < test grep " "                      --> needed error heandler

echo | cat -e                               --> stuck, dose note close write pipe

"root@mac:# unset PATH
pipes: 1
root@mac:# ls
"                                           --> dose not display correct error 

"ls filethatdoesntexist | grep bla | more"  --> dose not display correct error

"ls -sl ldl fllf lgl gl hlhl h"             --> dose not display correct error

export PATH=$PATH:/place/with/the/file

----------------------------------------------------------------------------------

ls > output | wc -l

"dsbksdgbksdghsd"                           --> dose not display correct error

"cat | cat | ls behave "normally""          --> dose not display correct error

"cat | cat | ls behave "normally"           --> dose not correct work               !!!!!!!!!!

"ec -l"                                     --> dose not return error 

"wc -l"                                     --> dose not work ctrl+d

grep " " <kek.txt                           --> dose not pars "<"



ls -al | grep ^- > kek.txt | wc -l
ls -la | wc >  test | wc -c
ls -la | wc >  test > test2 > test3 | wc -c



$SHLVL 
exit status: https://stackoverflow.com/c/42network/questions/1545
pipe: https://stackoverflow.com/c/42network/questions/1470